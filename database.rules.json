{
  "rules": {
    "users": {
      "$uid": {
        "settings": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "categories": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "calendar": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "shortcuts": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "todo": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}