# Test Scenarios: InterfaceManager

## Happy Path

1. [ ] Sets initial attributes on document.documentElement (theme, scheme, scale)
2. [ ] Sets initial CSS variables on document.documentElement (--ui-scale, --hour-height, --header-height)
3. [ ] Updates attributes when store state changes
4. [ ] Updates CSS variables when store state changes

## Edge Cases

1. [ ] Handles decimal values for scale/uiScale correctly (toString)
